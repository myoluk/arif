/** @type {import('next').NextConfig} */
const nextConfig = {
  // Product images use plain <img> tags served from the FastAPI backend.
  // next/image remote domain configuration is not required.
};

module.exports = nextConfig;
